import javax.swing.*;

/*
	Mock implementation for testing purposes.
	Delete this file when the actual class is pushed to main.
*/

class MenuBar extends JMenuBar {
	MenuBar(JPanel mainScreen) {
		JLabel content = new JLabel("Menu bar");
		this.add(content);
	}
}